# How to run

## Lambda
```
aws cloudformation create-stack --stack-name blablabla --template-body file://lambda.yaml --capabilities CAPABILITY_AUTO_EXPAND CAPABILITY_IAM --profile blablabla
aws cloudformation delete-stack --stack-name blablabla --profile blablabla


```







